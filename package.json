{
  "name": "batjs",
  "version": "0.2.0",
  "description": "A utility for extracting images of specimens from a directory and its subdirectories and batching them into folders of a specific number of specimens. The name a play on 'batches', and hints at the language used",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "batjs": "./index.js"
  },
  "author": "Ian Engelbrecht",
  "license": "ISC",
  "dependencies": {
    "commander": "^5.0.0"
  }
}
